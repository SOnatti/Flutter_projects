// this file contains constant features that we will use in the entire app
import 'package:flutter/material.dart';

class constant_values {
  final brand = Color.fromARGB(255, 220, 145, 255);
  final sec_brand = Color.fromARGB(255, 114, 146, 151);
  final blackMain = Color.fromARGB(255, 10, 10, 10);
}
